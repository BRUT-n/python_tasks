def sort_pair(pair):
    a, b = pair # pair переводится в кортеж
    return(min(a, b), max(a,b)) # возврат минмального и максимального значения

# как работает функция с вызовом не догнал, принял помощь чата гпт

print(sort_pair((4, 3))) # почему вызов функции через такое количество скобок? 